{
  "manifest_version": 1,
  "id": "workshop",
  "name": "Workshop",
  "summary": "A bench for refining one scene with an agent — two pads, dialed together until it's right.",
  "category": "create",
  "maker": { "name": "Mike Schwarz", "url": "https://github.com/mvschwarz" },
  "media": {
    "screenshots": [
      { "src": "assets/workshop-linkpix.png", "alt": "The Scene Workshop bench inside the studio-box app shell with the agent sidebar (a live operator seat; terminal content redacted for card privacy). One scene — m83 stutter x Prince Innocence — is open with two doctor-verified pad channels (volume, key, vibe, edited/original/keep), VERSIONS / KEY / MIX health, a 26-scene library, and Suggest / Match keys / Match space actions; the status line reads brief landed in the agent's session — watch the AGENT panel" }
    ]
  },
  "install": {
    "destination": "~/studio/apps/workshop",
    "surface": {
      "entry": "app/workshop.html",
      "path": "/workshop.html",
      "glyph": "▧",
      "hint": "Open one scene and refine it with the engineer-agent — key, mix, versions, all live in the studio shell"
    },
    "verify": [
      "The workshop surface runs in the studio shell with the engineer-agent sidebar; opening a scene loads its pads, its versions, and its KEY / MIX / EDITS health",
      "Suggest / Match keys / Match space / Match notes land a brief in the agent's session (watch the AGENT panel), and duplicating a version snapshots it — switching versions destroys nothing; the agent backend runs on the box, not in this package",
      "Compare the canonical installed source at ~/studio/apps/workshop against the box's served surface: repair (copy the canonical source in) ONLY if the box surface is missing, provably broken, or provably older; if the box surface is newer (forward drift) leave it untouched and report; if age or health is ambiguous, leave it untouched and report to the box lead agent — take no destructive action"
    ]
  },
  "verbs": [
    { "method": "GET",  "path": "/api/project",              "purpose": "Load the scene bench — the scene, its pads, versions, and health" },
    { "method": "POST", "path": "/api/ws/bounce",            "purpose": "Duplicate a version — a snapshot you can switch to and from freely" },
    { "method": "POST", "path": "/api/ws/keylock",           "purpose": "Match keys across the scene's pads" },
    { "method": "POST", "path": "/api/ws/matchnotes",        "purpose": "Match notes across the pads" },
    { "method": "POST", "path": "/api/ws/apply-suggestion",  "purpose": "Apply the agent's suggested change to the scene" },
    { "method": "POST", "path": "/api/inject",               "purpose": "Land a brief in the engineer-agent's session (Suggest / Match)" },
    { "method": "POST", "path": "/api/save",                 "purpose": "Save the refined scene" }
  ]
}
